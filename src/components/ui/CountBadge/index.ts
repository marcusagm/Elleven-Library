/**
 * CountBadge component module.
 *
 * @module CountBadge
 * @description
 * Specialized badge for displaying numeric counts with automatic shortening (1k, 1.2M)
 * and an integrated tooltip for the exact value.
 *
 * @example
 * ```tsx
 * import { CountBadge } from '@/components/ui';
 *
 * <CountBadge count={1234} variant="primary" />
 * ```
 */

export * from './CountBadge';
export * from './types';
