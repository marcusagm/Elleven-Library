/**
 * ColorInput component
 *
 * @module ColorInput
 * @description
 * The ColorInput component is a specialized Input component that allows the user to enter color values.
 * It provides a combined text field and graphical picker for color selection.
 *
 * @example
 * <ColorInput
 *   value="#ff0000"
 *   onChange={(value) => console.log(value)}
 *   disabled={false}
 *   class="custom-class"
 * />
 */
export * from './Root';
export * from './types';
