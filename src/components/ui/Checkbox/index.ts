/**
 * @module Checkbox
 * A customizable checkbox component supporting labels, descriptions, sizes, and indeterminate states.
 *
 * @example
 * ```tsx
 * import { Checkbox } from './Checkbox';
 *
 * <Checkbox label="Enable notifications" />
 * ```
 */

export * from './Checkbox';
export * from './types';
