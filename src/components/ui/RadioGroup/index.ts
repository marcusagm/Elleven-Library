/**
 * @module RadioGroup
 * A group of mutually exclusive radio buttons allowing single selection within a set.
 *
 * @example
 * ```tsx
 * import { RadioGroup } from './RadioGroup';
 *
 * <RadioGroup options={[{ value: '1', label: 'Option 1' }]} />
 * ```
 */

export * from './Root';
export * from './Item';
export * from './types';
